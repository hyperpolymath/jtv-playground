-- SPDX-License-Identifier: AGPL-3.0-or-later
-- invalid/missing_module.jtv - Missing module declaration (EXPECTED ERROR)

-- ERROR: JTV requires a module declaration at the top level

fn main() -> !:
    loop:
        nop()
